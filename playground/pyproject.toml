[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "big-dill-playground"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pytest",
    "pytest-bdd",
    "pytest-big-dill",
    "pyyaml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
bdd_features_base_dir = "features"
